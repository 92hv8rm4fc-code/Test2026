{
  "name": "pokemon-binder-pokedex-ios",
  "version": "0.1.0",
  "private": true,
  "description": "Pokemon Binder Pokedex — iOS app (Capacitor)",
  "scripts": {
    "cap:sync": "npx cap sync ios",
    "cap:open": "npx cap open ios",
    "ios:prepare": "npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.2",
    "@capacitor/ios": "^7.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2"
  }
}
